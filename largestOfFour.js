function largestOfFour(arr) {
	var max = [];
	for (var i = 0; i < arr.length; i++) {
		console.log(arr[i])

		
		using nested loop
		for (var j = 0; j < arr[i].length; j++) {
			console.log(arr[i][j]);
		}
	}
}